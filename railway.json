{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx puppeteer browsers install chrome || true; node dist/index.js"
  }
}
